<Window x:Class="OfficeWebshopAdminPanelApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="200" x:Name="EmailTextBox"/>
            <PasswordBox x:Name="PasswordBox" Width="200"/>
            <Button Content="Login" Command="{Binding LoginAsyncCommand}" Width="200"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red"/>
        </StackPanel>
    </Grid>
</Window>
