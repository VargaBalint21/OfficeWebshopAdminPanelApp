<Window x:Class="OfficeWebshopAdminPanelApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OfficeWebshopAdminPanelApp.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Email" />
        <TextBox Grid.Row="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Jelszó" Margin="0,10,0,0"/>
        <PasswordBox Grid.Row="3" x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>

        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>

        <Button Grid.Row="6" Content="Bejelentkezés" Height="30" Width="100" HorizontalAlignment="Center"
                Click="LoginButton_Click" />
    </Grid>
</Window>
